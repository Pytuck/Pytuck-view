# pytuck-view 开发计划

## 1. 表管理功能

### 1.1 表操作
- [x] 删除表
- [x] 清空表数据
- [x] 新建表

### 1.2 表结构编辑

目前仅支持修改列备注，需扩展为完整的表结构编辑：

- [ ] 修改列类型 ⏳ 需 pytuck 底层补充 `alter_column` API
  - int → str：自动转换
  - str → int：可转换则转换，不可转换（如 "abc"）则报错
- [ ] 修改主键设置 ⏳ 需 pytuck 底层补充 `set_primary_key` API
- [ ] 修改必填性（nullable） ⏳ 需 pytuck 底层补充 `alter_column` API
- [ ] 修改默认值 ⏳ 需 pytuck 底层补充 `alter_column` API
- [x] 添加新列
- [x] 删除列
- [ ] 修改列顺序（可选） ⏳ 需 pytuck 底层补充 `reorder_columns` API

**约束**：修改后若现有数据不满足新约束，应报错并拒绝变更。

---

## 2. 表搜索功能

- [ ] 左侧表列表支持搜索（前端过滤）
  - [x] 按表名搜索
  - [x] 按表备注搜索

---

## 3. 数据表视图优化

- [x] 支持横向滚动查看多列数据
  - 当前列数过多时表格无法完整显示
  - 需确保表头与数据列对齐

---

## 4. 数据导入导出

- [ ] 导出数据
  - 导出整表数据
  - 导出格式：CSV、JSON、Excel 等
- [ ] 导入数据
  - 从文件导入到现有表
  - 格式匹配与数据验证

---

## 5. 数据库文件转换

- [x] 在首页支持数据库文件格式转换
  - 在 pytuck 支持的引擎类型间互转
  - 如：binary ↔ json ↔ sqlite ↔ csv 等
  - 需要 pytuck 提供转换接口支持

---

## 6. 列筛选功能

- [ ] 在数据表格视图的表头上直接筛选
  - 输入搜索内容
  - 选择搜索方式（等于、包含、大于、小于等）
  - 支持多列组合筛选

---

## 优先级建议

### 近期（P0-P1）
1. **P0**：横向滚动（体验问题，影响基本使用）
2. **P1**：表搜索（易实现，提升效率）

### 中期（P2-P3）
3. **P2**：表结构完整编辑（工作量大，需 pytuck 配合）
4. **P3**：表管理功能（依赖表结构编辑完成）
5. **P3**：列筛选功能（提升数据查看效率）

### 远期（P4）
6. **P4**：数据导入导出
7. **P4**：数据库文件转换（需 pytuck 支持）

---

## 已知 pytuck 层面问题

> 以下问题需要在 pytuck 库中排查/修复，pytuck-view 暂无法处理。

- [ ] **引擎转换后数据丢失**：CSV → Binary 转换后文件体积增大，但打开转换后的文件只显示表名，无法查看数据。疑似 `migrate_engine` 在转换时 lazy loading 未正确触发，或目标后端 `save_full` 未持久化行数据。
- [ ] **flush/load 循环可能篡改数据**：添加带默认值的列后，flush + 重新 load 过程中，部分后端可能对缺失字段自动填充默认值，导致原本为 null 的记录被意外赋值。需确认各后端（binary/json/csv）的 save/load 行为一致性。
